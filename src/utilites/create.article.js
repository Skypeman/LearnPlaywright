/*
import { faker } from "@faker-js/faker";

const createArticle = (title = '', about = '', text = '', tags = '') => {
   const title = title || faker.food.dish();
   const about = about || faker.lorem.words({ min: 1, max: 10 });
   const text = text || faker.food.description();
   const tags = tags || faker.lorem.words({ min: 1, max: 3 });


   return {
      title,
      about,
      text,
      tags
   }
}
export { createArticle }

*/